*** Begin Patch
*** Update File: src/app/books/new/page.tsx
@@
-  const books = await db
-    .select()
-    .from(book)
-    .orderBy(desc(book.createdAt))
-    .limit(50);
+  const books = await db
+    .select({
+      id: book.id,
+      slug: book.slug,
+      title: book.title,
+      subtitle1: book.subtitle1,
+      subtitle2: book.subtitle2,
+      createdAt: book.createdAt,
+      comingSoon: book.comingSoon,
+    })
+    .from(book)
+    .orderBy(desc(book.createdAt))
+    .limit(50);
*** End Patch

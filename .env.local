cat > .env.local <<'ENV'
# AWS S3 (used by src/app/api/admin/uploads/route.ts)
AWS_S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=AKIAREPLACE_WITH_REAL
AWS_SECRET_ACCESS_KEY=REPLACE_WITH_REAL_SECRET
AWS_REGION=us-east-1

# site url for local dev (used by uploads route for disk fallback)
NEXT_PUBLIC_SITE_URL=http://localhost:3002
ENV

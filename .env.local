# create .env.local (do NOT commit this file)
cat > .env.local <<'ENV'
# AWS S3 (if you have a bucket and keys)
AWS_S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=AKIAREPLACE_WITH_REAL
AWS_SECRET_ACCESS_KEY=REPLACE_WITH_REAL_SECRET
AWS_REGION=us-east-1

# Local site url used by uploads route fallback
NEXT_PUBLIC_SITE_URL=http://localhost:3002
ENV
